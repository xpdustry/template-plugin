name: "Build (Java)"

on:
  push:
    branches: [ "**" ]
    tags-ignore: [ "**" ]
  pull_request:
  release:
    types: [ released, prereleased ]

jobs:
  build:
    uses: xpdustry/.github/.github/workflows/build-java.yaml@master
    with:
      java_version: 25
      artifacts_path: build/tmp/release/*.jar
      version_file: plugin.json
      version_file_kind: mindustry-mod
    secrets: "inherit"
